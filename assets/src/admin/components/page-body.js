/**
 * Settings page body wrapper.
 *
 * @param {Object} props          Component props.
 * @param {Object} props.children Body content.
 */
const PageBody = ({ children }) => (
	<div className="llc-page-body">{children}</div>
)

export default PageBody
